{
  "name": "discord-notification-bot",
  "version": "2.0.0",
  "main": "notification-bot.js",
  "engines": {
    "node": ">=26.0.0"
  },
  "scripts": {
    "send": "node --env-file-if-exists=.env notification-bot.js",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "author": "Jonathan Fenske",
  "license": "MIT",
  "description": "A personal notification bot.",
  "devDependencies": {
    "@types/node": "^26.1.1",
    "prettier": "^3.9.5"
  }
}
